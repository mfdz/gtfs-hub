storage:
  tsdb:
    retention:
      size: 1GB

scrape_configs:
  - job_name: pushgateway
    scrape_interval: 10s
    scrape_timeout: 5s
    honor_labels: true
    static_configs:
      - targets:
          - 'prometheus_pushgateway:9091'
